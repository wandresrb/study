---
interface Props { num?: string | number; title?: string; }
const { num, title } = Astro.props;
---
<div class="paso">
  {num != null ? <span class="paso-num">{num}</span> : null}
  <div class="paso-body">
    {title ? <div class="paso-title">{title}</div> : null}
    <slot />
  </div>
</div>
