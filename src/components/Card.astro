---
interface Props { icon?: string; title: string; }
const { icon, title } = Astro.props;
---
<div class="card">
  {icon ? <div class="c-ic">{icon}</div> : null}
  <h4>{title}</h4>
  <p><slot /></p>
</div>
