---
title: "vim.system: la forma moderna de lanzar un proceso"
description: "La puerta única para ejecutar programas externos desde Lua: modo síncrono con :wait() y asíncrono con on_exit, el resultado con code, signal, stdout y stderr, la manija del proceso vivo con write y kill, y las opciones que gobiernan el entorno del hijo: cwd, env, stdin, text, timeout y detach."
subject: neovim
level: 13
order: 1
posicion: "13.1"
duracion: "18 min"
---

<Lead>
Un editor que no sabe hablar con el sistema operativo es un procesador de textos caro. Todo lo que hace útil a Neovim en un flujo real —compilar, formatear, consultar `git`, invocar un linter— ocurre fuera de su proceso. Durante años ese diálogo se hizo con funciones de Vimscript heredadas, cada una con su propia forma de mentir sobre los errores. `vim.system` las sustituye a todas: una única puerta, con semántica explícita y sin sorpresas.
</Lead>

<Objetivos>

- Ejecutar un programa externo con `vim.system` en modo síncrono y asíncrono.
- Interpretar el resultado: `code`, `signal`, `stdout` y `stderr`, y no confundir los dos primeros.
- Manejar el proceso **vivo** con la manija: `write`, `kill`, `wait` e `is_closing`.
- Gobernar el entorno del hijo con `cwd`, `env`, `stdin`, `text`, `timeout` y `detach`.
- Saber por qué el modo asíncrono es el correcto por defecto, y qué pinta `vim.schedule` aquí.

</Objetivos>

## Una función, dos modos

`vim.system` recibe una **lista de argumentos**, no una cadena de shell. Esta decisión es la primera línea de defensa: no hay expansión de comodines, ni división por espacios, ni inyección posible a través de un nombre de archivo con caracteres raros. El primer elemento es el ejecutable; el resto son sus argumentos, uno por posición.

```lua
local obj = vim.system({ 'git', 'rev-parse', '--abbrev-ref', 'HEAD' }, { text = true }):wait()
print(obj.code, vim.trim(obj.stdout))
```

La contrapartida de saltarse el shell es que los *builtins* del shell no existen: `cd`, `export` o el `echo` interno de tu intérprete no son ejecutables. Si de verdad necesitas uno, invoca el shell explícitamente —`vim.system({ 'bash', '-c', 'help' })`—, sabiendo que reintroduces todo lo que la lista te ahorraba.

La llamada devuelve un objeto de proceso de inmediato. Encadenar `:wait()` lo convierte en **síncrono**: el bucle de eventos se detiene ahí hasta que el hijo termina. Es cómodo para un dato instantáneo, y es veneno para cualquier cosa que tarde: mientras esperas, Neovim no repinta, no atiende teclas y no responde al LSP.

<Predict question="Ejecutas :lua vim.system({ 'sleep', '5' }):wait() ¿Qué hace el editor durante esos cinco segundos?" options={[
  { t: 'Se congela por completo: no repinta, no atiende teclas, no responde al LSP', ok: true, x: 'wait() detiene el bucle de eventos, que es el único hilo que hace todo eso (12.1). El hijo corre en paralelo, pero tu editor está parado esperándolo. Por eso el modo síncrono se reserva para lo instantáneo.' },
  { t: 'Sigue respondiendo: vim.system es asíncrona por diseño', x: 'La llamada a vim.system sí retorna al instante; es el :wait() encadenado el que bloquea. Asíncrono de verdad es pasar un callback como tercer argumento y no llamar a wait.' },
  { t: 'Responde a teclas pero no repinta hasta que sleep termine', x: 'No hay término medio: entrada, repintado y callbacks viven todos en el mismo bucle de eventos. Si está detenido, está detenido para todo.' },
]} />

El mismo trabajo en **asíncrono** se hace pasando una función como tercer argumento. `vim.system` retorna al instante y tu callback se invoca cuando el proceso muere:

```lua
vim.system({ 'cargo', 'build' }, { text = true }, function(obj)
  vim.schedule(function()
    if obj.code == 0 then
      vim.notify('build ok')
    else
      vim.notify(obj.stderr, vim.log.levels.ERROR)
    end
  end)
end)
```

El `vim.schedule` no es decorativo. El callback corre en el *fast event context* que conociste en la lección 12.3: un momento del bucle donde la mayoría de la API de Neovim está prohibida. `vim.schedule` aplaza el cuerpo al siguiente instante seguro, y omitirlo produce el clásico `E5560: nvim_… must not be called in a fast event context`.

<Pipeline
  title="una llamada, dos modos"
  result="{ code, signal, stdout, stderr }: el veredicto completo del proceso"
  groups={[
    { title: 'síncrono — :wait()', tone: 'peach', sequence: true, steps: [
      { t: 'vim.system(cmd, opts)', d: 'Arranca el hijo y devuelve la manija al instante.' },
      { t: ':wait()', d: 'El bucle de eventos se detiene aquí hasta que el hijo muere.' },
      { t: 'resultado en la misma línea', d: 'Para datos instantáneos: un rev-parse, un which. Nada que tarde.' },
    ]},
    { title: 'asíncrono — on_exit', tone: 'blue', sequence: true, steps: [
      { t: 'vim.system(cmd, opts, on_exit)', d: 'Retorna de inmediato; el editor sigue libre.' },
      { t: 'on_exit(obj)', d: 'Se invoca al morir el hijo, en fast context: casi toda la API vetada.' },
      { t: 'vim.schedule(función)', d: 'Cruza al momento seguro antes de tocar buffers o notificar (12.3).' },
    ]},
  ]}
/>

## Anatomía del resultado

Tanto `wait()` como el callback entregan la misma tabla. Sus cuatro campos cuentan la historia completa de la vida del proceso.

<Cards>
  <Card icon="🔢" title="code">
    El código de salida. Cero significa éxito por convención universal de POSIX. Cualquier otro valor es un fallo cuyo significado define el programa.
  </Card>
  <Card icon="💀" title="signal">
    La señal que lo mató, si murió por señal. Un proceso terminado a la fuerza tiene `signal` distinto de cero, y su `code` deja de contar la historia real.
  </Card>
  <Card icon="📤" title="stdout">
    La salida estándar completa, acumulada. Es `nil` si la desactivaste o si registraste tu propio handler, que es el modo streaming de la lección siguiente.
  </Card>
  <Card icon="📛" title="stderr">
    El canal de diagnóstico. Muchas herramientas escriben aquí incluso cuando triunfan, así que no lo uses como sinónimo de error.
  </Card>
</Cards>

La regla operativa es simple: **`code` decide si hubo éxito, `stderr` explica por qué no lo hubo**. Confundir los dos es el error más frecuente al envolver herramientas.

<Predict question="Lanzas vim.system({ 'grep', 'PATRON_QUE_NO_EXISTE', 'a.txt' }):wait() y el archivo existe. ¿Qué recibes?" options={[
  { t: 'code = 1, stderr vacío: grep no falló, simplemente no encontró nada', ok: true, x: 'Para grep, 1 significa cero coincidencias y 2 significa error real. El código de salida es la convención de CADA herramienta, no un booleano universal. Antes de tratar code ~= 0 como fallo, lee su manual.' },
  { t: 'code = 0, stdout vacío: no encontrar nada no es un error', x: 'Sería un diseño razonable, pero no es el de grep: reserva el 0 para cuando SÍ hubo coincidencias, porque sus usuarios encadenan grep en scripts que preguntan exactamente eso.' },
  { t: 'Un error de Lua: vim.system lanza cuando el comando falla', x: 'vim.system solo lanza error cuando el proceso no puede ni ARRANCAR (binario inexistente, cwd inválido). Un proceso que arranca y devuelve código distinto de cero es un resultado normal, no una excepción.' },
]} />

## La manija del proceso vivo

El objeto que `vim.system` devuelve —antes de esperarlo— también tiene vida propia. Es una manija sobre el proceso en marcha, y expone justo lo necesario para gobernarlo desde fuera:

```lua
local proc = vim.system({ 'sort' }, { stdin = true, text = true }, on_done)
proc:write({ 'pera', 'manzana' })  -- una lista escribe cada elemento con su \n
proc:write(nil)                    -- cierra stdin: sort ya puede ordenar
print(proc.pid)                    -- identificador real en el sistema
-- proc:kill('sigterm')            -- si te arrepientes; también acepta el número 15
```

`write` distingue entre formas: con una **lista**, escribe cada elemento seguido de un salto de línea; con una **cadena**, la escribe tal cual, sin añadir nada; con **`nil`**, cierra el canal. Ese `write(nil)` merece un subrayado: cerrar la entrada estándar no es cortesía, es la señal que muchas herramientas esperan para empezar a trabajar. Un `sort` con `stdin` abierto no ordena nada porque, correctamente, sigue creyendo que puede llegar más entrada.

La manija completa: `pid`, `write`, `kill` —acepta la señal como número o como nombre—, `is_closing`, y un `wait(timeout)` con su propio matiz: si le pasas un límite y vence, el proceso recibe `SIGKILL`, no el `SIGTERM` educado de la opción `timeout`. Guardar la manija es lo que en la lección 13.5 te permitirá cancelar, matar al cerrar un buffer y evitar que dos ejecuciones compitan. Tirarla y quedarte solo con el callback funciona hasta el día en que necesitas interrumpir algo, y ese día llega siempre.

<Callout type="warn" title="text solo normaliza, no decodifica">
La opción `text = true` hace exactamente una cosa: reemplaza `\r\n` por `\n` en `stdout` y `stderr`. No interpreta la codificación. Si la herramienta emite algo que no es UTF-8 válido, recibirás esos bytes tal cual, y Neovim no podrá insertarlos limpiamente en un buffer. Para salida binaria —un archivo comprimido, una imagen— omite `text` y trata el resultado como lo que es.
</Callout>

## El entorno del hijo

Un proceso hereda de su padre más de lo que parece: el directorio actual, las variables de entorno, la entrada estándar. Dejar esa herencia al azar es la causa número uno de plugins que funcionan en la máquina de su autor y en ninguna otra.

```lua
vim.system({ 'rg', '--json', 'TODO' }, {
  cwd = vim.fs.root(0, '.git'),          -- raíz del repositorio, no el cwd de la ventana
  env = { RIPGREP_CONFIG_PATH = '' },    -- neutraliza la config personal del usuario
  text = true,
  timeout = 5000,                        -- milisegundos
}, on_done)
```

`cwd` merece atención especial. El directorio de trabajo de Neovim puede cambiar por ventana o por pestaña, así que anclarlo explícitamente a la raíz del proyecto hace el comportamiento reproducible. `env` **añade** al entorno heredado —que además llega con `NVIM` apuntando al servidor del editor padre, detalle que los procesos hijos pueden usar para hablarle—; si quieres partir de cero, acompáñalo de `clear_env = true`.

`stdin` acepta tres formas, y conviene decir primero cuál es el defecto: **si no lo pides, el hijo no recibe tubería de entrada**, que es lo correcto para casi todo. Con una cadena o una lista de líneas, Neovim escribe ese contenido y cierra el canal él solo: es la forma cómoda de pasarle un buffer a un formateador sin tocar el disco. Con `true`, el canal queda abierto para que escribas tú con `write`, y cerrarlo con `write(nil)` pasa a ser responsabilidad tuya.

`timeout` es la única red de seguridad barata contra un proceso que no termina. Al vencer, Neovim envía `SIGTERM` y fija `code` en `124`, la misma convención que la utilidad `timeout` de coreutils. Sin él, un comando colgado se queda colgado para siempre.

Queda `detach`, la opción que casi nunca quieres: arranca el hijo como líder de su propio grupo de procesos, con la consecuencia de que sobrevivirá al cierre de Neovim. Es lo correcto para lanzar un demonio de verdad, y es una fábrica de huérfanos para todo lo demás; la lección 13.5 le pasa factura.

## Lo que sustituye

Neovim arrastra tres generaciones de API para lo mismo, y saber cuál estás leyendo es parte de saber leer configuraciones ajenas.

| Generación | Función | Problema |
|---|---|---|
| Vimscript | `system` y `systemlist` | Síncrona, pasa por el shell, mezcla `stderr` y no distingue señales |
| Job control | `jobstart` y `jobwait` | Asíncrona y correcta, pero de bajo nivel y con callbacks por trozos |
| Moderna | `vim.system` | Los dos modos, resultado estructurado, cierre y limpieza automáticos |

La propia ayuda de `jobstart` lo dice hoy en su primera línea: *desde Lua, prefiere `vim.system`, salvo que necesites `rpc`, `pty` o `term`*. Esas excepciones existen —un buffer de terminal necesita la opción `term`, y la verás en la lección 13.3—, pero para el noventa por ciento de los casos, lanzar algo, esperar su veredicto y reaccionar, `vim.system` es la respuesta correcta y la única que deberías escribir en código nuevo.

Si heredas una configuración antigua, la traducción es casi mecánica:

```lua
-- antes
local salida = vim.fn.systemlist('git status --porcelain')
if vim.v.shell_error ~= 0 then return end

-- después
local obj = vim.system({ 'git', 'status', '--porcelain' }, { text = true }):wait()
if obj.code ~= 0 then return end
local salida = vim.split(obj.stdout, '\n', { trimempty = true })
```

Fíjate en lo que desaparece con el cambio: la variable global `vim.v.shell_error`, que era estado compartido y por tanto imposible de usar en código asíncrono; el paso por el shell, con su citación y sus comodines; y la ambigüedad sobre dónde acabó `stderr`. Lo que ganas no es sintaxis más limpia, es un contrato que se puede razonar en concurrencia.

<Callout type="dios" title="El proceso como frontera de confianza">
Cuando llamas a `vim.system` no estás llamando a una función: estás cruzando la frontera más dura que existe en tu programa. Al otro lado hay un espacio de direcciones distinto, con su propio planificador, su propia tabla de descriptores y su propia idea de qué significa fallar. Esa frontera impone tres asimetrías que ninguna abstracción puede borrar. La primera es **temporal**: el hijo no tiene ninguna obligación de terminar, y todo el diseño de la API —el `timeout`, el `kill`, el modo asíncrono— existe para que esa libertad no se convierta en tu bloqueo. La segunda es **de canal**: `stdout` y `stderr` no son campos de un objeto sino tuberías con búfer del kernel, y el objeto plano que recibes es una ficción cómoda construida acumulando trozos que llegaron en un orden que tú no controlas. La tercera, y la más subestimada, es **de contrato**: el único vocabulario compartido entre padre e hijo es un entero de ocho bits y dos flujos de bytes sin estructura. Todo lo demás —que `1` significa no encontrado, que el formato de salida es estable, que `stderr` solo lleva errores— es convención local de cada herramienta, no garantía del sistema. Por eso la lista de argumentos importa más que la comodidad de una cadena de shell: al pasar una lista eliminas un intérprete entero de la cadena de confianza, y con él la clase completa de fallos que nacen de que alguien llame a un archivo con un espacio o un punto y coma en el nombre. Un plugin robusto no es el que llama bien al proceso; es el que trata cada uno de esos tres huecos como un caso a manejar, no como un detalle a ignorar.
</Callout>

<Callout type="info" title="Lo esencial">
`vim.system` recibe una lista de argumentos y devuelve una manija de proceso. Con `:wait()` bloquea; con un callback como tercer argumento no bloquea, y ese callback debe envolver en `vim.schedule` cualquier acceso a la API. El resultado trae `code`, `signal`, `stdout` y `stderr`: `code` decide el éxito, `stderr` lo explica. La manija sabe `write` —`nil` cierra stdin—, `kill` e `is_closing`. Fija siempre `cwd` y `timeout`.
</Callout>

<Exercise challenge="Averigua la rama actual de Git sin salir del editor, en modo síncrono." keys=":lua local o = vim.system({'git','rev-parse','--abbrev-ref','HEAD'},{text=true}):wait() print(o.code == 0 and vim.trim(o.stdout) or 'sin repo')">
Síncrono y legítimo: un `rev-parse` tarda milisegundos. La comprobación de `code` evita imprimir el `stderr` de Git como si fuera una rama.
</Exercise>

<Exercise challenge="Comprueba en vivo qué produce un timeout: lanza un proceso lento con límite de un segundo." keys=":lua vim.system({'sleep','10'},{timeout=1000},function(o) print(o.code, o.signal) end)">
Imprime `124 15`: el `code` de la convención de coreutils y el `SIGTERM` con que Neovim lo terminó. Fíjate en que el editor no se congeló en ningún momento: el callback llegó solo.
</Exercise>

<Exercise challenge="Ordena tres frutas alimentando a sort por stdin, sin archivos de por medio." keys=":lua vim.system({'sort'},{stdin={'pera','manzana','kiwi'},text=true},function(o) vim.schedule(function() vim.notify(o.stdout) end) end)">
La lista en `stdin` se escribe con un salto de línea por elemento y el canal se cierra solo. El `vim.schedule` es obligatorio: `vim.notify` toca la API y el callback corre en fast context (12.3).
</Exercise>

<Exercise challenge="Demuestra que el modo asíncrono no bloquea: lanza algo lento y sigue escribiendo mientras corre." keys=":lua vim.system({'sleep','3'},{},function() vim.schedule(function() vim.notify('terminó') end) end)">
Tres segundos moviendo el cursor con normalidad, y la notificación llega al final. Ese contraste con el primer drill es la lección entera en dos comandos.
</Exercise>

<Reto title="Tu primer envoltorio honesto">
1. Escribe una función `rama_actual` que use `vim.system` con `:wait()` y devuelva el nombre de la rama de Git, o `nil` si `code` no es cero.
2. Reescríbela en modo asíncrono, recibiendo un callback, y comprueba con `vim.notify` que el editor sigue respondiendo mientras corre.
3. Lanza `sleep 10` con `timeout = 1000` e imprime `code` y `signal`. Explica qué valor toma cada uno y por qué.
4. Ejecuta `grep` sobre un patrón inexistente y comprueba que `code` vale `1` sin que haya ocurrido ningún error real.
5. Alimenta a `sort` por `stdin` con las líneas del buffer actual —`nvim_buf_get_lines` te las da como lista— y muestra el resultado con `vim.notify`, sin escribir nada en disco.
6. Guarda la manija de un `sleep 30` en una variable global, comprueba `is_closing()`, mátalo con `kill('sigterm')` y vuelve a comprobarla.
</Reto>
