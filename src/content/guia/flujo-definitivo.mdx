---
title: "El flujo definitivo y mantener el nivel"
description: "Cómo encaja todo en un día real de desarrollo, cómo mantener sana tu config (checkhealth, performance, actualizaciones) y la mentalidad para no dejar de crecer."
level: 3
order: 5
icon: "3.5"
duracion: "12 min"
---

<Lead>
Has construido las piezas. Esta lección las une: cómo se siente un día real de desarrollo con todo funcionando, cómo mantener el setup sano y afilado, y qué mentalidad separa a quien "usa Neovim" de quien lo ha convertido en una extensión de su mente.
</Lead>

<Objetivos>

- Ver el flujo completo integrado en un día de trabajo.
- Mantener la config sana: checkhealth, performance, actualizaciones.
- Hábitos para seguir mejorando sin romper nada.

</Objetivos>

## Un día de nivel dios

<Mermaid code={`flowchart LR
  S["Terminal<br/>z proyecto"] --> Z["Multiplexor<br/>sesión + layout"]
  Z --> N["nvim + leader ff"]
  N --> E["Editar: gramática Vim<br/>+ Treesitter + flash"]
  E --> L["LSP: gd, K, leader ca<br/>+ blink.cmp"]
  L --> AI["IA: ghost text<br/>+ refactor revisado"]
  AI --> G["lazygit: commit"]
  G --> D["nvim-dap: depurar si falla"]
  D --> E
  style S fill:#a6e3a1,color:#11111b
  style N fill:#89b4fa,color:#11111b
  style AI fill:#cba6f7,color:#11111b
  style G fill:#fab387,color:#11111b`} />

Un flujo real, sin tocar el ratón:

1. Abres el terminal, `z api` te lleva al proyecto. Tu sesión de zellij/tmux revive con el editor, el servidor y los logs donde los dejaste.
2. `<leader>ff` para abrir el archivo, o `<leader>fg` para buscar esa función a medias recordada.
3. Editas con la gramática de Vim y objetos de Treesitter: `cif`, `daf`, `S` para saltar, `<leader>rn` para renombrar seguro.
4. El LSP te guía: `gd` navega, `K` documenta, `<leader>ca` arregla. blink.cmp completa; el ghost text sugiere lo obvio.
5. Un refactor mayor: seleccionas, `<leader>ai`, revisas el diff, aceptas.
6. `<leader>gg` (lazygit) para el commit. Si algo falla, `<leader>db` + `<leader>dc` para depurarlo en vivo.
7. Navegas al panel de logs con <KeyCap keys="Ctrl-j" /> sin pensar si es Neovim o el multiplexor.

Todo encadenado, a velocidad de pensamiento.

## Mantén la config sana

<Cards>
  <Card icon="🩺" title=":checkhealth">
    Ejecútalo tras cambios grandes o actualizaciones. Detecta dependencias rotas, providers y problemas de LSP.
  </Card>
  <Card icon="⏱️" title=":Lazy profile">
    Mide el tiempo de arranque. Si algo lo dispara, revisa su lazy-loading (event/ft/keys).
  </Card>
  <Card icon="🔄" title=":Lazy sync">
    Actualiza plugins. Hazlo con calma, no a diario: revisa el changelog si algo cambia de comportamiento.
  </Card>
  <Card icon="🔒" title="lazy-lock.json">
    Versiónalo en Git. Es la foto exacta de tu setup: reproducible en cualquier máquina.
  </Card>
</Cards>

<Callout type="tip" title="Versiona tu config en Git">
Tu `~/.config/nvim` es tu obra. Ponlo en un repositorio: así experimentas sin miedo (siempre puedes volver atrás), lo replicas en cualquier ordenador, y llevas tu entorno contigo. Incluye el `lazy-lock.json`.
</Callout>

## Rendimiento

Si Neovim se siente lento:

- `:Lazy profile` para ver qué plugin cuesta al arrancar y afinar su carga perezosa.
- `:checkhealth` para detectar un LSP mal configurado que reindexa de más.
- En archivos gigantes, Treesitter y algunos plugins pueden pesar; existen plugins como `bigfile` (en snacks) que los desactivan automáticamente.

## La mentalidad para no estancarse

<Callout type="dios" title="Nunca terminas, y esa es la gracia">
Neovim no es un producto acabado: es un jardín que cultivas. La diferencia entre un usuario avanzado y uno de nivel dios no es saberlo todo — es el hábito de mejorar una cosa cada semana. ¿Repetiste un movimiento incómodo tres veces? Búscate un objeto de texto o un atajo mejor. ¿Un plugin nuevo resuelve algo que hacías a mano? Pruébalo un día. Pequeñas mejoras compuestas durante meses te llevan a un dominio que ningún IDE cerrado puede darte, porque es **tuyo**.
</Callout>

Para seguir creciendo:

- `:help` es la mejor documentación de cualquier editor. `:help ciw`, `:help :g`, `:help lsp`. Vive ahí.
- Lee configs ajenas (LazyVim, kickstart.nvim, dotfiles en GitHub) y roba ideas.
- La comunidad: r/neovim, [awesome-neovim](https://github.com/rockerBOO/awesome-neovim) para descubrir plugins, y [dotfyle.com](https://dotfyle.com) para ver qué usan otros.

## Lo lograste

Empezaste sin saber salir del editor. Ahora dominas la gramática modal, construiste tu propia config en Lua, montaste un IDE para C, Rust, Swift y frontend, orquestas la terminal con multiplexores, automatizas lo repetitivo e integras IA con criterio. Eso es nivel dios.

Ten a mano el [Cheatsheet](/cheatsheet/) para consultar y la [Config completa](/config/) como plantilla. Y recuerda: la única forma de mantener el nivel es usarlo cada día.

<Reto title="Tu ritual de mantenimiento">
1. Convierte tu `~/.config/nvim` en un repo Git (incluye `lazy-lock.json`).
2. Ejecuta `:checkhealth` y resuelve cualquier aviso pendiente.
3. Mira tu `:Lazy profile` y anota el plugin más caro.
4. Elige **una** cosa que hagas de forma incómoda y busca en `:help` una manera mejor.
5. Vuelve aquí dentro de un mes y repite el paso 4. Ese es el bucle infinito del nivel dios.
</Reto>
